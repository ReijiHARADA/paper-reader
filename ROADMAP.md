# Paper Reader ロードマップ

> Version 0.3 / 2026-09-04  
> プロダクト原則と、現行実装では未達の項目の基準文書  
> **いま動いていること**は [README.md](./README.md) を正とする

Cursor は実装判断で README と衝突したら README を優先し、これから作る機能では本書を優先する。技術上の都合だけで、下のプロダクト原則は変えない。

## 0. 文書の役割

| 文書 | 役割 |
|---|---|
| [README.md](./README.md) | 現行実装。起動方法、画面、データ保存、翻訳エンジン |
| [ROADMAP.md](./ROADMAP.md) | 変えない原則、未達、これから必要なこと、後回し |

初期開発用の Phase 0 プロトタイプ指示は役目を終えたので削除した。

## 1. プロダクト原則

英語論文 PDF を、日本語の Web 記事を読む感覚で読める、ローカル動作のデスクトップリーダー。

PDF の見た目を複製するのではなく、論文の意味構造を維持して読書向けに再構成する。

維持する対象:

- 論文タイトル、著者、出版情報
- セクションとサブセクション
- 段落の順序と境界
- 原文段落と翻訳段落の 1 対 1 対応
- 図、表、キャプションと本文中の位置関係
- 数式、引用番号、脚注、参考文献
- 元 PDF 上のページ情報

原則として維持しない対象:

- 2 段組などの誌面レイアウト
- フォント、改ページ、余白の忠実な再現
- 日本語訳を元 PDF と同じ位置へ流し込むこと

運用ルール:

1. **日本語を主役にする** — 通常は訳文だけを本文にする。英語を常時横並びしない
2. **原文は一操作で確認できる** — 別画面へ遷移させない
3. **読書中の UI を減らす** — 常設チャット欄、過剰なツールバー、編集用 UI は置かない
4. **処理途中でも読める** — 全体完了を待たない
5. **ローカルファースト** — PDF・抽出・翻訳・読書位置は端末内。外部 API を必須にしない

## 2. すでに満たしていること

詳細は README。ここは残作業の前提だけ書く。

- Tauri 2 の macOS `.app`。MADLAD サイドカーを同梱起動
- ライブラリ（All Papers / Inbox / Recently Read）と Project（ドラッグで所属、多対多）
- 日本語 1 カラム、原文インライン展開、アウトライン、⌘F 検索、表示設定、読書位置復元
- 訳文選択のハイライトと Notes
- タイトル → Abstract → 本文、読書位置付近を優先する文バッチ翻訳
- スキャン PDF の Apple Vision OCR（Tauri のみ）
- 図画像の抽出・本文内表示・拡大
- 元 PDF のアプリ内複製と別表示オープン
- 未完了翻訳の再開、段落の再翻訳、抽出低信頼の警告
- 合成 PDF による読み順回帰テスト（`npm test`）

## 3. 未達・これから必要なこと

優先度は読書体験への影響順。上から着手する。

### 3.1 抽出と読み順（最優先）

2 段組は合成フィクスチャでは左列→右列になるが、実論文では列が混ざる。図・キャプション・見出し・参考文献の境界もまだ崩れる。

残作業:

- 実論文 PDF での読み順を、合成フィクスチャ以外でも測れるようにする（実ファイルはリポジトリに入れない）
- ヘッダー／フッター／著作権の本文混入をさらに減らす
- 見出しが段落に吸収されるケース、参考文献見出しの取りこぼしを減らす
- 図キャプションが本文に混ざる／本文がキャプション扱いになるケースを減らす
- 読み順が崩れた箇所は、低信頼表示と元 PDF 導線を確実に出す
- パスワード保護 PDF は処理せず理由を出す（現状は専用の失敗分岐がない）

テスト方針は維持する。種類ごとの確認点:

| 種類 | 確認点 |
|---|---|
| 1 カラム・文字中心 | 基本抽出、見出し、段落 |
| 2 段組 | 左列を読み終えてから右列 |
| 図が多い | 図とキャプションの位置 |
| 表が多い | 表の欠落と順序 |
| 数式が多い | 数式の欠落・改変なし |
| 引用が多い | 引用記号と References |
| 50〜100 ページ | UI が固まらない、途中再開 |
| スキャン PDF | OCR 後に読める。失敗時は案内 |

同一の少数 PDF だけに最適化しない。

### 3.2 リーダーに出していない構造

内部型にはあるが、本文に出ていない／作っていないもの。

| 要素 | 現状 | 必要なこと |
|---|---|---|
| 表 | `table` ブロックは作るが、`PaperContent` が描画しない | キャプションと表画像（またはテキスト）を本文位置に出す。キャプションは翻訳する |
| 数式 | `Equation` コンポーネントはあるが、構造化が `equation` ブロックをほぼ作らない | インライン／ブロック数式を翻訳対象から外し、順序を保つ。取れない場合は元 PDF 領域の画像 |
| 脚注 | レイアウト上は本文と混ぜないテストがある。`footnote` ブロックは作らない | 本文中の位置関係を保って出し、原文展開できるようにする |
| 図キャプション | 原文のまま表示。`captionTranslated` は埋まらない | キャプションを翻訳し、原文を展開できるようにする |
| 引用リンク | `[12]` 等は文字列として残るだけ | 紐付けに自信があるときだけ References へ飛ぶ。自信がなければリンクしない |
| 図中文字 | 未着手 | MVP 外。オンデマンド翻訳は将来候補 |

図そのものの翻訳はしない（キャプションはする）。

### 3.3 翻訳の一貫性と用語集

現状: Ollama があれば用語集を IndexedDB に保存する。MADLAD には渡さない。`applyGlossary` は未使用。前後段落・セクション名も渡していない。

残作業:

- 生成した用語集を翻訳前後のどちらかで実際に使う
- 用語集の閲覧と、採用訳の手動修正 UI（データは編集可能な形で既にある）
- 段落再翻訳時に用語集を反映する
- タイトル／見出し／キャプションの訳抜けをなくす（キャプションは 3.2）

翻訳単位は 1 段落のまま。保存結果は現在段落だけとし、原文との 1 対 1 を崩さない。

### 3.4 ライブラリと Project

動いている所属操作以外:

- **Favorites**: 画面と `Paper.favorite` はあるが、スターを付ける操作がない。リーダーかカードから付け外しできるようにする
- **サイドバー検索**: 入力欄があるが論文一覧を絞り込まない。タイトル・著者・訳文／原文の絞り込みが必要
- **読書進捗**: カードは最終閲覧日だけ。セクションまたはブロック進捗を出す
- **重複 PDF**: ハッシュ一致で打ち切るが、既存論文を開く導線が弱い。通知して既存を開く
- **Project メタデータ**: `researchQuestion` / `keywords` / 所属の status・decision・tags は型だけ。UI は名前と説明の作成に留める。必要になるまで UI を増やさない
- **論文カードの一時停止**: 処理中の一時停止は未実装。キャンセルより再開できる停止の方がよい

論文削除はアプリ内データと複製 PDF を消す。OS 上の原本は消さない（現行どおり）。削除時に原本まで消すかは聞かない。原本はコピー元でありアプリの所有物ではない。

### 3.5 インポート UX

- 「準備できた部分から読む」はある。**キャンセル**と失敗時の**同じファイルでの再試行**がない
- 失敗は工程名を出しているが、スキャン検出・翻訳サーバー未起動・容量不足などをユーザー向けに分けきれていない
- 初回の MADLAD 重み取得（約 6GB）の案内が起動待ち画面だけ。導入中であることが分かる短い説明が必要
- インポート画面にトークン数や内部ログを出さない（現行どおり）

区別すべき失敗:

- PDF を開けない
- テキストを抽出できない
- 読み順を推定できない
- OCR が必要（または OCR 失敗）
- 構造推定の一部が低信頼
- 翻訳エンジンを起動できない
- 特定段落だけ翻訳できない
- 保存容量が不足

原則: 「失敗しました」だけで終わらせない。一部失敗でも成功済みは読める。原本 PDF は変更しない。

### 3.6 元 PDF 参照

複製して `open` で開くところまで実装済み。ページ番号は API にあるが無視している。

残作業:

- 現在ブロックの元ページへ開く（macOS Preview のページ指定）
- 低信頼・抽出失敗箇所から、そのブロックのページへ戻る
- 可能なら bounding box 付近が分かる表示。無理ならページ単位でよい

### 3.7 性能・アクセシビリティ

- 長い論文を一度に DOM へ載せている。100 ページ級で操作不能にならないよう、遅延描画または仮想化を検討する
- 主要操作のキーボード対応は ⌘F と Escape 程度。アウトライン開閉、原文展開、ライブラリ移動を足す
- アウトラインは常時表示。閉じられるようにする。処理中セクションの状態表示もない
- Option キー押下中だけ原文を出す案は実験機能のまま。必須にしない
- アイコンだけの操作にはラベルまたはツールチップを付ける（一部は済）

### 3.8 設定と配布

- 設定モーダルは MADLAD / Ollama の接続確認が中心。Ollama 説明の「要約・質問応答」は未実装なので、用語集に合わせて文言を直す
- 正式なプロダクト名とアイコン
- ライセンス表記
- App Store / 公証付き配布
- Windows は対象外のまま

## 4. 今はやらないこと

利用実態を見るまで着手しない。常設 AI チャットは今後も置かない。AI を足すなら選択箇所への短い操作にする。

- 図表画像内 OCR と図中文字の翻訳
- DeepL のような元レイアウト維持型 PDF 書き出し
- Word、EPUB、HTML 入力
- 手書きメモ
- 論文要約、RQ 抽出、批判的評価
- クラウド同期、複数端末同期、共同編集
- 選択文の説明、「この文の役割」などの文脈理解支援
- 参考文献の外部リンク
- 読書メモの Markdown 書き出し
- 論文メタデータの横断自動取得
- Windows 版

## 5. 残作業の受け入れ条件

次を満たしたら、現行アプリの「読める」品質を一段上げたとみなす。

1. 代表的な 2 段組論文で、左列を読み終えてから右列になり、図キャプションが本文に混ざらない
2. 表が本文中の位置に見え、キャプションの日本語が確認できる
3. 図キャプションが日本語で出る。原文キャプションを一操作で展開できる
4. 数式と引用番号が翻訳で欠落・改変されない（取れない数式は画像または原文フォールバック）
5. 用語集が翻訳に反映されるか、少なくとも論文内で参照・修正できる
6. Favorites に論文を入れたり外したりできる
7. 元 PDF が、いま読んでいるブロックのページ付近で開く
8. 翻訳または抽出の一部が失敗しても、成功済み部分を読める（現行を壊さない）
9. PDF および翻訳データが初期設定で外部へ送信されない（現行を壊さない）

## 6. 未確定事項

後から変えてよい。原則（日本語主役、ローカルファースト、途中から読める）は変えない。

- 用語集を翻訳プロンプトに載せるか、訳後置換にするか
- 表を画像切り出しにするか、テキスト表にするか
- 仮想化の単位（セクションかブロックか）
- サイドバー検索の対象範囲（メタデータだけか、本文も含むか）
- 公証と配布チャネル
