# Paper Reader ロードマップ

> Version 0.4 / 2026-09-04  
> プロダクト原則と、現行実装では未達の項目の基準文書  
> **いま動いていること**は [README.md](./README.md) を正とする

Cursor は実装判断で README と衝突したら README を優先し、これから作る機能では本書を優先する。技術上の都合だけで、下のプロダクト原則は変えない。実装を変えたら README と本書を同じ作業で更新し、できたことは未達に残さない。

## 0. 文書の役割

| 文書 | 役割 |
|---|---|
| [README.md](./README.md) | 現行実装。起動方法、画面、データ保存、翻訳エンジン |
| [ROADMAP.md](./ROADMAP.md) | 変えない原則、未達、これから必要なこと、後回し |

初期開発用の Phase 0 プロトタイプ指示は役目を終えたので削除した。

## 1. プロダクト原則

英語論文 PDF を、日本語の Web 記事を読む感覚で読める、ローカル動作のデスクトップリーダー。

PDF の見た目を複製するのではなく、論文の意味構造を維持して読書向けに再構成する。

維持する対象:

- 論文タイトル、著者、出版情報
- セクションとサブセクション
- 段落の順序と境界
- 原文段落と翻訳段落の 1 対 1 対応
- 図、表、キャプションと本文中の位置関係
- 数式、引用番号、脚注、参考文献
- 元 PDF 上のページ情報

原則として維持しない対象:

- 2 段組などの誌面レイアウト
- フォント、改ページ、余白の忠実な再現
- 日本語訳を元 PDF と同じ位置へ流し込むこと

運用ルール:

1. **日本語を主役にする** — 通常は訳文だけを本文にする。英語を常時横並びしない
2. **原文は一操作で確認できる** — 別画面へ遷移させない
3. **読書中の UI を減らす** — 常設チャット欄、過剰なツールバー、編集用 UI は置かない
4. **処理途中でも読める** — 全体完了を待たない
5. **ローカルファースト** — PDF・抽出・翻訳・読書位置は端末内。外部 API を必須にしない

## 2. すでに満たしていること

詳細は README。ここは残作業の前提だけ書く。

- Tauri 2 の macOS `.app`。MADLAD サイドカーを同梱起動。ライブラリはサーバー待ちせずすぐ出す
- ライブラリ（All Papers / Inbox / Favorites / Recently Read / Project）。論文カードは共通レイアウト。一覧の横幅は Project と同じく画面幅いっぱいに出す。All Papers / Inbox / Project の追加はタイトル右の「論文を追加」。Project はドラッグで所属（多対多）
- 設定は左サイドバー最下部から、ライブラリと同じ画面遷移で開く。目次は一般 / 翻訳 / 読書 / ストレージ / 診断。サンプル論文は一般、表示設定はリーダーと共通、翻訳キャッシュ削除はストレージ
- 日本語 1 カラム、原文インライン展開、アウトライン、⌘F 検索（矢印 / Enter でヒット間移動）、表示設定、読書位置復元
- 訳文選択で「メモを追加」を出し、クリック後に Notes。Notes と用語集の右パネルは排他
- タイトル → Abstract → 本文、読書位置付近を優先する文バッチ翻訳（既定 24、micro-batch 合流）。日本語論文は翻訳せずレイアウトのみ
- スキャン PDF の Apple Vision OCR（Tauri のみ）。日本語 CID フォントは pdf.js CMap で抽出する
- 1 ページ目の最大フォントを論文タイトルにし、Abstract / `1 はじめに` までをマストヘッドとして著者・所属・英語副題を本文見出しにしない。日本語名とローマ字著者を取る
- 図・表の抽出・本文内表示・拡大。英語の Figure/Table に加え日本語の `図 n:` / `表 n:` もキャプションにする。キャプションは翻訳し、原文を展開できる。横並びの別図は埋め込み画像を重なり最大のキャプションへ 1 対 1 で割り当て、隣図と合成しない
- 日本語の節番号（`1 はじめに`、`2.1` など）と `参考文献` を見出し／目次にする。番号付き箇条書きは見出しにしない
- 検出できたブロック数式は翻訳せず原文のまま出す。脚注は本文位置に出し原文を展開できる
- `[12]` は参考文献が1件に特定できるときだけジャンプする
- 元 PDF のアプリ内複製と別表示オープン。低信頼箇所から元 PDF へ誘導する
- 未完了翻訳の再開、段落の再翻訳、抽出低信頼の警告
- 用語集の生成（Ollama 任意）、訳後置換、リーダーでの閲覧・修正
- パスワード付き PDF は処理せず理由を出す
- 読み順回帰: 合成 PDF（`test-fixtures/`）と実論文キャッシュ（`test-data/real-papers/`、リポジトリには入れない）。catalog に publisher / formatFamily を GT として持てる。Format Profile detector は import 未接続の PoC
- ハイフン連結の英文と参考文献 URL をブロック数式にしない
- CCS / Index Terms などの分類カタログ行は訳さず原文のまま出す（「翻訳待ち」に残さない）
- 翻訳失敗バナーは、本文で再試行できる段落だけを数える。画面に出ない見出しブロックの失敗は出さない。論文カードの「一部失敗」も同じ基準
- プロジェクト画面の「論文を追加」で PDF をその Project に入れる。削除は右上のゴミ箱と確認ダイアログ
- Finder から PDF をアプリ画面へドロップしてインポートできる（Tauri のファイルドロップ。Project 画面ならその Project に所属する）

## 3. 未達・これから必要なこと

優先度は読書体験への影響順。上から着手する。

### 3.1 抽出と読み順

代表的な ACM 2 段組（Interactive Jewellery など）では左列→右列になり、図キャプションは独立ブロックになる。実論文コーパスでの回帰もある。

調査と独立 PoC（Format Profile、検出閾値、GROBID TEI、native 文字列の bbox 割当、benchmark runner）は [ACADEMIC_PDF_EXTRACTION_RESEARCH.md](./ACADEMIC_PDF_EXTRACTION_RESEARCH.md)。**現行 `pdfLayout.ts` は Production parser のまま。** import は切り替えていない。GROBID / Docling / MinerU は同梱しない。

まだ崩れること:

- 実論文では列混在・見出し吸収・参考文献見出しの取りこぼしが残る。同一の少数 PDF だけに最適化しない。壊れた PDF のたびに `pdfLayout.ts` へ if を足さない
- ヘッダー／フッター／著作権の本文混入は減ったが、論文タイトル型のランニングヘッダーなどは残る
- 図キャプションと本文の取り違いは減ったが、ゼロではない
- 所属が 2×2 のグリッドだと、複数機関が同じ行に連結されることがある（本文見出しにはしない）。著者↔所属 relation は未実装
- `Section.parentSectionId` は未配線
- インライン数式や、キャプションに見えにくい表はまだ落とす
- 50〜100 ページで UI が固まらないことは未検証（3.7）
- Format Profile を Production の補正に使うのは、回帰が全部通り baseline より明確に良くなってから

テスト方針は維持する。種類ごとの確認点:

| 種類 | 確認点 |
|---|---|
| 1 カラム・文字中心 | 基本抽出、見出し、段落 |
| 2 段組 | 左列を読み終えてから右列 |
| 図が多い | 図とキャプションの位置 |
| 表が多い | 表の欠落と順序 |
| 数式が多い | 数式の欠落・改変なし |
| 引用が多い | 引用記号と References |
| 50〜100 ページ | UI が固まらない、途中再開 |
| スキャン PDF | OCR 後に読める。失敗時は案内 |

実ファイルはリポジトリに入れない。カタログは `test-fixtures/real-papers/catalog.json`、PDF は gitignore の `test-data/real-papers/`。catalog の formatFamily は評価用であり、本番の format 判定に使わない。

### 3.2 リーダー構造の残差

表・ブロック数式・脚注・図／表キャプション翻訳・自信があるときの引用リンクは出した。図そのものは訳さない（現行どおり）。

まだ足りないもの:

| 要素 | 現状 | 必要なこと |
|---|---|---|
| 数式 | ブロックとして取れた行は原文表示。インラインは本文に残る。ハイフン英文・URL の誤検出は止めた。PDF 領域の画像フォールバックは弱い | 取れない数式は元 PDF 領域の画像にする |
| 引用リンク | 番号が参考文献と 1 対 1 のときだけ飛ぶ | 範囲 `[1–3]` や曖昧な番号はリンクしない（現行）。誤リンクを増やさない |
| 図中文字 | 未着手 | MVP 外。オンデマンド翻訳は将来候補 |

### 3.3 翻訳の一貫性と用語集

用語集は訳後置換で使い、リーダーから参照・修正できる。再翻訳にも載せる。MADLAD のプロンプトには載せない。

残作業:

- タイトル／見出しの訳抜けをさらに減らす
- 前後段落・セクション名はまだ渡していない（翻訳単位は 1 段落のまま）

保存結果は現在段落だけとし、原文との 1 対 1 を崩さない。

### 3.4 ライブラリと Project

動いている所属操作以外:

- **Favorites**: 画面と `Paper.favorite` はあるが、スターを付ける操作がない。リーダーかカードから付け外しできるようにする
- **サイドバー検索**: 入力欄があるが論文一覧を絞り込まない。タイトル・著者・訳文／原文の絞り込みが必要
- **読書進捗**: カードは最終閲覧日だけ。セクションまたはブロック進捗を出す
- **重複 PDF**: ハッシュ一致で打ち切るが、既存論文を開く導線が弱い。通知して既存を開く
- **Project メタデータ**: `researchQuestion` / `keywords` / 所属の status・decision・tags は型だけ。UI は名前と説明の作成に留める。必要になるまで UI を増やさない
- **論文カードの一時停止**: 処理中の一時停止は未実装。キャンセルより再開できる停止の方がよい

論文削除はアプリ内データと複製 PDF を消す。OS 上の原本は消さない（現行どおり）。削除時に原本まで消すかは聞かない。原本はコピー元でありアプリの所有物ではない。

### 3.5 インポート UX

- 「準備できた部分から読む」はある。**キャンセル**と失敗時の**同じファイルでの再試行**がない
- 失敗は工程名を出しているが、スキャン検出・翻訳サーバー未起動・容量不足などをユーザー向けに分けきれていない
- 初回の MADLAD 重み取得（約 6GB）の案内が起動待ち画面だけ。導入中であることが分かる短い説明が必要
- インポート画面にトークン数や内部ログを出さない（現行どおり）

区別すべき失敗:

- PDF を開けない
- テキストを抽出できない
- 読み順を推定できない
- OCR が必要（または OCR 失敗）
- 構造推定の一部が低信頼
- 翻訳エンジンを起動できない
- 特定段落だけ翻訳できない
- 保存容量が不足

原則: 「失敗しました」だけで終わらせない。一部失敗でも成功済みは読める。原本 PDF は変更しない。

### 3.6 元 PDF 参照

複製して `open` で開くところまで実装済み。リーダーはブロックの `pageStart` を渡すが、macOS の `open` はページを無視する。

残作業:

- 現在ブロックの元ページへ開く（macOS Preview のページ指定）
- 低信頼箇所からの導線は出した。ページ単位でそのブロックへ戻す
- 可能なら bounding box 付近が分かる表示。無理ならページ単位でよい

### 3.7 性能・アクセシビリティ

- 長い論文を一度に DOM へ載せている。100 ページ級で操作不能にならないよう、遅延描画または仮想化を検討する
- 検索ヒット間の移動（↑↓ / Enter）は対応済み。アウトライン開閉、原文展開、ライブラリ移動のキーボードは未対応
- アウトラインは常時表示。閉じられるようにする。処理中セクションの状態表示もない
- Option キー押下中だけ原文を出す案は実験機能のまま。必須にしない
- アイコンだけの操作にはラベルまたはツールチップを付ける（一部は済）

### 3.8 設定と配布

- 設定は左サイドバー最下部の画面。一般 / 翻訳 / 読書 / ストレージ / 診断の目次。MADLAD / Ollama の接続確認とサンプル論文追加、表示設定はリーダーと共有
- 正式なプロダクト名とアイコン
- App Store / 公証付き配布
- Windows は対象外のまま
- 本番 CSP。WKWebView で Vite の ES module が止まって白い画面になったため、いったん未設定。アプリ本体と `127.0.0.1` / `localhost` だけを許可する方針で再導入する

## 4. 今はやらないこと

利用実態を見るまで着手しない。常設 AI チャットは今後も置かない。AI を足すなら選択箇所への短い操作にする。

- 図表画像内 OCR と図中文字の翻訳
- DeepL のような元レイアウト維持型 PDF 書き出し
- Word、EPUB、HTML 入力
- 手書きメモ
- 論文要約、RQ 抽出、批判的評価
- クラウド同期、複数端末同期、共同編集
- 選択文の説明、「この文の役割」などの文脈理解支援
- 参考文献の外部リンク
- 読書メモの Markdown 書き出し
- 論文メタデータの横断自動取得
- Windows 版

## 5. 残作業の受け入れ条件

次を満たしたら、現行アプリの「読める」品質を一段上げたとみなす。

1. 代表的な 2 段組論文で、左列を読み終えてから右列になり、図キャプションが本文に混ざらない（代表例は満たす。全論文ではない）
2. 表が本文中の位置に見え、キャプションの日本語が確認できる
3. 図キャプションが日本語で出る。原文キャプションを一操作で展開できる
4. 検出できた数式と、紐付けできる引用番号が翻訳で欠落・改変されない（取れない数式の画像フォールバックは未完）
5. 用語集が訳後置換で使われ、論文内で参照・修正できる
6. Favorites に論文を入れたり外したりできる
7. 元 PDF が、いま読んでいるブロックのページ付近で開く
8. 翻訳または抽出の一部が失敗しても、成功済み部分を読める（現行を壊さない）
9. PDF および翻訳データが初期設定で外部へ送信されない（現行を壊さない）

## 6. 未確定事項

後から変えてよい。原則（日本語主役、ローカルファースト、途中から読める）は変えない。

- 用語集は訳後置換にした。プロンプトに載せる案は採っていない
- 表はキャプション位置の画像切り出し。テキスト表はまだない
- 仮想化の単位（セクションかブロックか）
- サイドバー検索の対象範囲（メタデータだけか、本文も含むか）
- 公証と配布チャネル
